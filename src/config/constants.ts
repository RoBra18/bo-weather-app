/**
 * Constantes globales de la aplicación (unidades, configuraciones por defecto, etc.)
 */
export const DEFAULT_UNITS = 'metric';

export const DEFAULT_CITY = {
  name: 'Buenos Aires',
  country: 'AR',
  lat: -34.6037,
  lon: -58.3816,
};
